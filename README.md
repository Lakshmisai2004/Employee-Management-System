# Employee-Management-System
The Employee Management System is a Java-based console application that performs CRUD operations using JDBC and MySQL. It allows users to add, view, update, and delete employee records efficiently. The project applies the DAO design pattern, OOP concepts, and exception handling for clean and modular code management.
